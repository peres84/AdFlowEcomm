# Runware API Configuration
# Get your API key from: https://my.runware.ai/

# Required: Your Runware API key
RUNWARE_API_KEY=your_runware_api_key_here

# Optional: Other API keys (if using other services)
OPENAI_API_KEY=your_openai_api_key_here
MIRALO_API_KEY=your_miralo_api_key_here

# Optional: Custom timeout settings (in milliseconds)
# Uncomment and adjust as needed

# IMAGE_INFERENCE_TIMEOUT=120000        # 2 minutes (default)
# VIDEO_POLLING_TIMEOUT=1440000         # 24 minutes (default)
# AUDIO_POLLING_TIMEOUT=240000          # 4 minutes (default)

# Instructions:
# 1. Copy this file to .env (or rename it)
# 2. Replace the placeholder values with your actual API keys
# 3. The .env file is in .gitignore and will not be committed to version control
